# Local build artifacts
/build/
*.src.rpm
*.rpm
*.log

# Upstream sources fetched at build time (spectool -g)
*.tar.gz
*.tar.xz
*.tgz
*.jar
*.dmg
*.zip
# evbt ships its release asset as a bare file named "evbt"
evbt/evbt
# Upstream license/notice texts fetched as extra sources into a package dir
/*/LICENSE
/*/COPYING
/*/COPYING.txt
/*/NOTICE

# Gradle / build caches
.gradle/
build/libs/

# Editor / OS noise
.DS_Store
*.swp
*~
.idea/
.vscode/
.claude/
